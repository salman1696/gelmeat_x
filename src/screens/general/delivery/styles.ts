import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        height: 100
    },
    fourBoxes: {
        display: 'flex',
        
    }
});

export default styles;
